# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mcp-odata-reporting
services:  
  app:
    language: dotnet
    project: ./mcpODataReporting.AppHost/mcpODataReporting.AppHost.csproj
    host: containerapp
