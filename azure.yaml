# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mcp-odata-reporting
services:
  odataapi:
    language: dotnet
    project: ./ODataApi/ODataApi.csproj
    host: containerapp
  
  mcpodatareporting:
    language: dotnet
    project: ./MCP/McpODataReporting.csproj
    host: function  # ← Explicitly define as Function
